// src/pages/Units/UnitsListPage.jsx
// Units list page - Page Layer

import React from 'react';
import UnitsContainer from './UnitsContainer';

/**
 * Units List Page
 * Page Layer: Route params wrapper
 * Renders the container which handles data fetching
 */
export default function UnitsListPage() {
	return <UnitsContainer />;
}
